# --- Entorno virtual de Python (se recrea con requirements.txt) ---
.venv/
venv/

# --- Caché y compilados de Python ---
__pycache__/
*.py[cod]
.pytest_cache/

# --- Datos locales: NO se versionan (son datos personales, no código) ---
# La protección de estos datos la da el respaldo automático en backups/.
*.db
backups/

# --- Copia de código previa al refactor (el historial de git ya la reemplaza) ---
_backup_codigo_2026-06-13/

# --- Sistema operativo ---
.DS_Store

# --- Claude Code / asistente de IA (configuración local, no se versiona) ---
.claude/
**/.claude/
.claude_cache/
CLAUDE.md
**/CLAUDE.md

# --- Documentación de contexto: local, no se versiona en el repo ---
docs/
